# 登录 / 注册与三角色体系 · 实施规划与实施记录

> 状态：**四步全部完成**（2026-09-16）。本文档同时作为实施记录，验收数据见第七节与 `README.md` 第十节。
> 关联文档：`REQUIREMENTS.md`（需求基线，已同步改写口径）、`PLAN.md`（阶段计划）

---

## 一、定位与口径（先定这个，否则答辩会自相矛盾）

这是**课程原型内的演示级账号体系**，不追求生产级安全。三条硬口径：

1. **与「内存数据源」定位自洽**：用户表、会话表都在内存，**后端重启后需重新登录、自注册账号消失**——这句话直接写进答辩口径，是加分点而不是缺陷。
2. **密码明文存储**（原型不做加密），报告与答辩必须**主动说明**，否则被问到就是硬伤。
3. **必须同步改文档**：`REQUIREMENTS.md` 里现在写着"不实现用户注册登录""无鉴权"，要做三处口径更新（需求范围、非功能需求的安全性、不在实现范围内清单），并在 5 视图架构中新增认证/权限相关内容。

---

## 二、角色与权限矩阵

| 角色 | 能力 | 页面范围 |
| --- | --- | --- |
| **普通用户**（C 端） | 浏览门店/酒店、下单、查看与取消**自己的**订单 | 现有 8 个页面 |
| **商家** | 查看**自己门店/酒店**的经营概览与订单（只读为主），维护营业时间/简介 | 新增 `/merchant` 后台 |
| **管理员** | 查看全平台概览、用户列表（可停用/启用账号）、全平台订单 | 新增 `/admin` 后台 |

权限规则：接口按角色鉴权 → 前端按角色渲染入口 → 未登录访问受限页跳登录页并回跳。

---

## 三、数据模型（新增 2 个对象，改造 3 个）

| 对象 | 关键字段 |
| --- | --- |
| **User**（新增） | `id`、`username`、`password`（明文，标注仅演示）、`role`(user/merchant/admin)、`nickname`、`phone`、`status`(正常/停用)、`createdAt`、`merchantId`（商家绑定的门店或酒店 ID） |
| **Session**（新增） | `token`、`userId`、`createdAt`（内存字典，重启即失效） |
| Order（改造） | 增加 `userId`（订单归属），下单时写入，查询时按 token 过滤 |
| Shop / Hotel（改造） | 增加 `merchantId`（商家只能看自己的门店/酒店） |
| 预置账号 | `demo/123456`（普通用户）、`shangjia/123456`（商家，绑定 S001 蜀大侠火锅）、`admin/123456`（管理员） |

> 预置订单继续保留，并绑定到 `demo` 账号 —— 这样"换个账号登录，订单列表就变了"可以直接演示。

---

## 四、接口清单（新增 10 个，改造 2 个）

### 认证（4 个）
| 接口 | 说明 |
| --- | --- |
| `POST /api/auth/register` | 用户注册（仅注册普通用户；用户名重复校验） |
| `POST /api/auth/login` | 登录 → 返回 `{token, user}`；账号停用时拒绝登录 |
| `POST /api/auth/logout` | 注销（删除内存会话） |
| `GET /api/auth/me` | 用 token 取当前用户（前端刷新页面恢复登录态） |

### 用户（2 个，改造现有）
| 接口 | 说明 |
| --- | --- |
| `GET /api/user/orders`（改造 I8） | 只返回**当前用户**的订单 |
| `POST /api/user/orders/{orderNo}/cancel`（改造 I10） | 只能取消**自己**的订单 |

### 商家（3 个）
| 接口 | 说明 |
| --- | --- |
| `GET /api/merchant/summary` | 我的门店/酒店：评分、评价数、订单数、待使用数 |
| `GET /api/merchant/orders` | 我名下的订单列表 |
| `PUT /api/merchant/profile` | 修改营业时间/简介（唯一写操作） |

### 管理员（4 个）
| 接口 | 说明 |
| --- | --- |
| `GET /api/admin/overview` | 平台概览：门店数、酒店数、用户数、订单数 |
| `GET /api/admin/users` | 用户列表（含角色、状态） |
| `POST /api/admin/users/{id}/status` | 停用 / 启用账号 |
| `GET /api/admin/orders` | 全平台订单 |

---

## 五、前端改动

| 改动 | 说明 |
| --- | --- |
| 新增 `Login.vue` | 登录 / 注册二合一（Tab 切换），带**「一键填入演示账号」**三选一按钮（用户 / 商家 / 管理员） |
| 新增 `MerchantHome.vue` | 商家后台：数据概览卡 + 我的订单表 + 门店信息编辑 |
| 新增 `AdminHome.vue` | 管理员后台：平台概览 + 用户表（停用/启用）+ 全平台订单 |
| 路由 | 新增 `/login`、`/merchant`、`/admin`；路由 `meta.requiresAuth` + `meta.roles` |
| 全局守卫 | 未登录 → `/login?redirect=`；角色不符 → 提示并回首页 |
| Axios 拦截器 | 自动附加 `Authorization`；401 清会话跳登录；403 提示无权限 |
| NavBar / Profile | 显示当前昵称与角色；**仅商家/管理员显示后台入口**；退出登录 |
| TabBar | 按角色变化（商家/管理员显示后台入口 Tab） |

---

## 六、答辩演示动线（新增的 4 分钟）

1. 注册新用户 → 登录 → 下单 → 个人中心看到自己的订单
2. 退出 → 换预置 `demo` 登录 → 订单列表不同（**证明订单归属隔离**）
3. 用商家账号登录 → 只看得到自己门店的订单
4. 用管理员账号登录 → 看到全平台数据 → **停用某账号** → 该账号无法登录
5. 主动说明：账号与会话存于内存，重启失效；密码明文仅演示，生产需加盐哈希 + JWT + 权限中间件

---

## 七、实施步骤与完成情况

| 步骤 | 内容 | 验收方式 | 状态 |
| --- | --- | --- | --- |
| **1** | 后端账号与会话：User/Session + register/login/logout/me | 在 `/docs` 直接注册、登录、拿 token、查 me | ✅ 完成（`check_auth.py` 认证部分） |
| **2** | 后端权限与角色：订单按用户过滤、商家/管理员接口 + 角色校验 | 越权访问被拒（401/403）用例 | ✅ 完成（`check_auth.py` 权限部分 + `check_api.py` 越权用例） |
| **3** | 前端登录注册页 + 守卫 + 拦截器 + 导航用户态 | "未登录点个人中心 → 跳登录 → 登录后回跳" | ✅ 完成（`tests/auth.spec.js` 16 项，含路由守卫用例） |
| **4** | 商家后台 + 管理员后台 + 文档口径同步 + 演示动线联调 | 端到端登录/角色动线；全部自检绿 | ✅ 完成（`e2e_demo_flow.py` 44 项含三角色动线） |

### 实施结果（实测数据）

| 自检 | 结果 |
| --- | --- |
| `check_auth.py`（认证 + 角色权限 + 越权 + 停用生效） | **41 / 41 通过** |
| `check_api.py`（I1~I10 + 未登录 401 + 越权 403 + 归属过滤） | **45 / 45 通过** |
| `e2e_demo_flow.py`（含登录 → 下单 → 三角色动线） | **44 / 44 通过** |
| `selfcheck.py`（数据层） | **33 / 33 通过** |
| 前端测试（渲染 + 登录态 + 守卫 + 会话） | **42 / 42 通过** |

### 实施中的偏差说明（与规划的差异，值得写进报告）

1. **未新增 `/api/user/orders` 系列接口**：规划中打算新建用户维度路径，实施时改为**保留原 I8/I9/I10 路径但加登录与归属校验**，前端调用路径不变、改动面更小，能力等价。
2. **商家绑定采用「商家 ID」而非「门店 ID」**：`User.merchantId = M001` 对应 `Shop.merchantId = M001`，支持一个商家拥有多个门店/酒店（实施中曾因两侧口径不一致（写成 S001/M001）导致商家查不到门店，由自检用例发现并修正）。
3. **注册后直接返回令牌**（自动登录），少一次登录操作，便于演示。

---

## 八、明确不做（写进文档，避免被追问）

短信/邮箱验证码、密码加密与找回、第三方登录、token 过期与刷新、记住我、按钮级细粒度授权、商家入驻审核流程、真实数据写入（后台以只读查询为主）。

---

## 九、影响面与工作量（提前告知）

| 项目 | 说明 |
| --- | --- |
| 受影响的自检脚本 | `check_api.py`、`e2e_demo_flow.py` 必须同步改造（订单接口需 token），否则会失败 |
| 需求文档 | `REQUIREMENTS.md` 三处口径 + 数据规模表（新增 User/Session） |
| 架构文档 | 5 视图：逻辑架构加认证层与权限层、数据架构加 User/Session 表设计、运行架构加登录与鉴权时序、物理架构不变 |
| 估计工作量 | 后端约 350 行、前端约 600 行、测试约 150 行（约 2 个工作日） |
| 建议 | 商家/管理员后台**先做只读**（除商家改简介），避免为"可编辑"再引入一批写接口与校验逻辑 |
