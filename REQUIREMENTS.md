# 项目需求说明与交付范围（Requirements & Deliverables）

> 本文件用于**冻结我对你需求的理解**，作为后续开发、文档、答辩的统一基准。
> 本文档只描述「要做什么」，不涉及三人分工方案（分工仅在 D12 答辩口径中作说明）。
>
> 项目：《软件架构与应用开发实践》课程实训原型系统
> 定位：**仿大众点评 + 携程的本地生活旅行综合演示原型系统**
> 工作目录：`D:\Ctrip`

---

## 一、我理解的需求（一句话）

你要的是一个**能现场跑起来、能按流程演示、能支撑 5 视图架构答辩**的课程原型系统，而不是一个商用级产品；因此工作重心在**前端 UI 与交互还原度**，后端只做**极简骨架 + 内存模拟数据 + 少量接口**。

---

## 二、我的交付物清单（Definition of Done）

### 1. 代码交付物

| 序号 | 交付物 | 说明 | 完成判定标准 |
| --- | --- | --- | --- |
| D1 | 前端工程 | 前后端分离的前端项目（页面、路由、公共组件、UI 风格对齐大众点评/携程） | 一条命令启动，浏览器可访问 |
| D2 | 极简后端工程 | 基础服务骨架 + 内存数据集 + 少量演示接口 | 一条命令启动，接口可被前端调通 |
| D3 | 内存模拟数据 | 门店、酒店、套餐、房型、评论、订单等预设数据 | 各列表页有数据、有图、有评分；数据量满足筛选/排序可演示（见第三节数据规模下限） |
| D4 | 虚拟环境 | Python 虚拟环境（`.venv`），**仅用于后端 FastAPI 依赖隔离**；前端项目使用 npm 管理依赖，不使用该虚拟环境 | 可激活、可安装依赖、后端能够正常启动运行 |
| D5 | 启动说明 | 如何激活虚拟环境、启动后端、启动前端、演示顺序 | 他人照做即可跑通 |

### 2. 文档交付物

| 序号 | 交付物 | 说明 |
| --- | --- | --- |
| D6 | 实训报告素材 | 引言、整体功能概述、系统功能需求、非功能需求、接口需求、**5 视图架构设计**、系统框架、功能设计、非功能设计、软件原型系统设计 |
| D7 | 需求与用例 | 五大模块功能需求 + 用例说明 |
| D8 | 领域对象模型 | 门店、酒店、评论、订单、套餐、房型等对象及关系，并注明**原型阶段存放于后端内存集合、未持久化** |
| D9 | 业务表设计思路 | 完整数据库表设计（字段、主键、外键、关系），并注明「生产版本可迁移至数据库」 |
| D10 | 多套技术方案对比 | 方案1 纯前端 Mock 无后端 vs 方案2 极简后端骨架（本项目选用），分析优劣与选型理由 |
| D11 | 5 视图架构说明 | 逻辑架构、开发架构、数据架构、运行架构、物理架构 |
| D12 | 答辩口径 | 原型限制说明、高频提问与回答要点；包含团队分工说明，要求全体成员熟悉 5 视图架构核心内容，应对老师随机提问 |

### 3. 演示交付物

现场检查需要能按以下流程一路演示通过（**端到端可复现**）：

1. 启动后端服务 → 2. 启动前端项目 → 3. 首页浏览、搜索跳转 → 4. 本地生活业务（门店筛选、详情、套餐、评论）→ 5. 旅行业务（酒店筛选、日历选时间、房型查看）→ 6. 执行模拟预约下单 → 7. 个人中心查看模拟订单、模拟取消预约 → 8. 口头说明原型限制：内存存储，服务重启新增订单丢失；本原型无登录注册、无商家与管理后台，仅用于课程演示。

---

## 三、业务功能全集（不考虑技术实现）

### 模块 1：首页
- 首页内容展示：轮播推荐位、业务分类入口、推荐**美食门店 / 酒店**卡片
- 业务分类入口：美食、休闲娱乐（本地生活）、酒店（旅行住宿）
- 搜索功能：关键词检索门店、酒店
- 分类导航：点击分类跳转至对应业务列表页面

### 模块 2：本地生活（对标大众点评）
- **门店列表**：图片、名称、评分、人均、业务标签；按评分/价格/品类筛选；按人气/评分排序
- **门店详情**：基础信息、营业时间、地址；团购套餐信息；模拟用户评论

### 模块 3：旅行住宿（对标携程）
- **酒店列表**：图片、名称、星级、价格、服务标签；按价格/星级/设施筛选；日历选择入住与退房时间；按价格/好评优先排序
- **酒店详情**：酒店图文信息；不同房型、房价、配套服务；模拟酒店评价

### 模块 4：预约 & 模拟下单
- 选择套餐或房型 → 填写姓名、手机号 → 选择预约/入住时间 → 提交 → 预约成功反馈
- **明确不做**：真实支付、消息通知、服务端信息校验与短信验证（**前端保留基础必填校验**，避免提交空表单，但不做真实校验逻辑）

### 模块 5：个人中心
- 查看模拟预约订单列表（预置订单 + 本次运行期间新下的订单）
- 查看订单详情
- 模拟取消预约：**在后端内存集合中将订单状态改为「已取消」**，并给出界面反馈；不落库、服务重启后状态与新增订单一并丢失

### 原型数据规模（保障筛选/排序能演出效果）

| 数据对象 | 规模下限 | 目的 |
| --- | --- | --- |
| 门店 | ≥ 20 家（成都 12 / 重庆 4 / 西安 4，含多品类、多价位） | 品类/城市/价格/评分筛选与排序要有可见变化 |
| 酒店 | ≥ 16 家（成都 10 / 重庆 3 / 西安 3，含多星级、多价位） | 城市/星级/价格/设施筛选与排序要有可见变化 |
| 城市 | 3 个（成都 / 重庆 / 西安） | 支撑城市切换与"本地生活"定位 |
| 团购套餐 | ≥ 3 个/门店（实际 4 个） | 门店详情套餐区不空 |
| 房型 | ≥ 3 种/酒店（实际 4 种） | 酒店详情房型区不空 |
| 评论 / 评价 | ≥ 10 条/门店、≥ 10 条/酒店 | 评论区可滚动、可体现评分分布 |
| 预置订单 | 2 ~ 3 条（分别归属 demo 与 lisi 账号） | 首次进入个人中心即有数据，不依赖现场下单；换账号登录可见不同订单 |
| 图片素材 | 每个门店/酒店 1 张封面 + 3 张轮播（共 39 张）；详情页图库暂不下载（`gallery` 字段保留，需要时一键补齐） | 本地化存储于 `frontend/public/images/`（离线可用），来源与授权见同目录 `CREDITS.md`；加载失败自动回退渐变色块占位图 |
| 用户账号 | 预置 4 个（普通用户 2 / 商家 1 / 管理员 1），支持注册普通用户 | 支撑"订单归属隔离、商家只看自己门店、管理员停用账号"三类演示 |

#### 非功能需求（原型系统）
1. 浏览器兼容性：基于Chrome主流版本完成演示，保证页面交互正常；
2. 性能：本地运行，页面加载、筛选跳转无明显卡顿，满足现场答辩演示；
3. 易用性：UI交互逻辑对标大众点评、携程，操作符合用户使用习惯；
4. 原型约束：业务数据仅保存在内存，后端重启后新增模拟预约订单、自注册账号与登录会话全部丢失；
5. 可维护性：前后端分离，公共组件复用（导航栏、底部栏、星级、弹窗、卡片），接口与页面解耦，便于扩展；
6. 安全性（演示级，已实现并需主动说明）：具备**演示级账号体系**——注册（仅普通用户）/ 登录 / 注销 / 当前用户查询，**按角色的数据权限**（普通用户仅自己的订单、商家仅自己名下门店/酒店的订单、管理员全平台），未登录访问受保护接口返回 401、越权返回 403；但**口令为明文存储、令牌无签名无有效期、会话仅存内存**，生产版本必须替换为加盐哈希 + JWT/会话中间件 + HTTPS。

#### 账号与角色（演示级，补充需求）

| 角色 | 能力 | 入口 |
| --- | --- | --- |
| 普通用户（user） | 浏览免登录；**下单、个人中心需登录**；仅可查看与取消自己的订单 | 前端底部「我的订单」 |
| 商家（merchant） | 查看自己名下门店/酒店的概览与订单；可维护营业时间与简介 | 底部「商家后台」 |
| 管理员（admin） | 查看平台概览、用户列表、全平台订单；可停用/启用账号（停用即失效其所有会话） | 底部「管理后台」 |

> 预置账号：`demo`（普通用户，名下有 2 条预置订单）、`lisi`（普通用户，1 条）、`shangjia`（商家，商家 ID `M001`，名下门店 S001）、`admin`（管理员），口令均为 `123456`。详细实施记录见 `AUTH_PLAN.md`。

---

## 四、技术实现约束（不可越界）

### 后端约束
1. 只搭**极简后端骨架**，以**内存集合**存模拟演示数据，**不做完整数据库持久化**
2. 接口以**查询接口为主**，非查询接口仅 **2 个**：模拟提交下单接口、模拟取消预约接口
3. **模拟下单接口增强**：接收到预约请求后，将订单对象追加至后端内存集合；仅当前服务运行期间可查询；后端服务重启，新增订单全部丢失，不持久化。
4. **模拟取消预约接口**：同样只修改内存集合中订单的状态字段，不落库；服务重启后状态丢失。
5. 文档中仍要写完整业务表设计思路，并注明「原型用内存数据，生产可迁移数据库」

### 前端约束
1. 重点还原**大众点评 + 携程** UI 风格
2. 实现页面跳转、筛选、日历、弹窗等交互
3. **复用公共组件**（导航栏、底部栏、星级、通用弹窗、卡片），减少重复开发
4. 表单做基础必填校验（姓名、手机号、时间非空），不做真实校验规则

### 前后端分离
- 浏览器发起 HTTP 请求访问本地后端服务，**无需部署到远程服务器**（物理架构即本地运行架构）

#### 接口需求

本系统全部接口为 REST 风格 HTTP 接口，前端通过 Axios 调用后端服务；统一响应结构为 `{ code, message, data }`。

| 编号 | 接口名称 | 方法 | 路径 | 主要入参 | 返回数据 | 对应页面 |
| --- | --- | --- | --- | --- | --- | --- |
| I1 | 首页推荐接口 | GET | `/api/home/recommend` | 无 | 轮播位、分类入口、推荐门店、推荐酒店 | 首页（分类导航复用本接口数据） |
| I2 | 关键词搜索接口 | GET | `/api/search` | `keyword` | 匹配的门店列表 + 酒店列表 | 首页搜索栏 |
| I3 | 门店列表接口 | GET | `/api/shops` | `keyword`、`category`、`minRating`、`maxPrice`、`sortBy` | 门店列表 | 本地生活列表页 |
| I4 | 门店详情接口 | GET | `/api/shops/{id}` | `id` | 门店基础信息 + 团购套餐 + 用户评论 | 门店详情页 |
| I5 | 酒店列表接口 | GET | `/api/hotels` | `city`、`minPrice`、`maxPrice`、`star`、`facility`、`checkIn`、`checkOut`、`sortBy` | 酒店列表 | 旅行住宿列表页 |
| I6 | 酒店详情接口 | GET | `/api/hotels/{id}` | `id` | 酒店图文信息 + 房型房价 + 酒店评价 | 酒店详情页 |
| I7 | 模拟预约下单提交接口 | POST | `/api/orders` | **需登录**；`type`(shop/hotel)、`targetId`、`itemId`(套餐/房型)、`name`、`phone`、`bookTime` | 成功提示 + 订单号（订单记录归属用户） | 预约下单弹窗 |
| I8 | 我的订单列表接口 | GET | `/api/orders` | **需登录**；按角色过滤（普通用户仅自己 / 商家仅自己门店 / 管理员全部） | 订单列表 | 个人中心、商家后台、管理后台 |
| I9 | 订单详情接口 | GET | `/api/orders/{orderNo}` | **需登录**；`orderNo`，含归属校验（越权返回 403） | 订单详情 | 个人中心订单详情页 |
| I10 | 模拟取消预约接口 | POST | `/api/orders/{orderNo}/cancel` | **需登录**；`orderNo`，含归属校验 | 成功提示 + 新状态「已取消」 | 个人中心 |

#### 账号与权限接口（A / M / D 三组）

| 编号 | 接口名称 | 方法 | 路径 | 说明 |
| --- | --- | --- | --- | --- |
| A1 | 用户注册 | POST | `/api/auth/register` | 仅开放普通用户角色；用户名 3~20 位、密码 ≥6 位 |
| A2 | 登录 | POST | `/api/auth/login` | 返回 `{token, user}`；账号停用时拒绝登录 |
| A3 | 注销 | POST | `/api/auth/logout` | 删除内存会话，令牌立即失效 |
| A4 | 当前用户 | GET | `/api/auth/me` | 前端刷新页面恢复登录态 |
| M1~M3 | 商家概览 / 我的订单 / 维护门店信息 | GET / GET / PUT | `/api/merchant/*` | 角色：merchant（管理员可代看概览与订单） |
| D1~D4 | 平台概览 / 用户列表 / 停用启用账号 / 全平台订单 | GET / GET / POST / GET | `/api/admin/*` | 角色：admin |

所有需登录接口通过请求头 `Authorization: Bearer <token>` 传递令牌。

本原型无对接第三方外部系统，不存在外部接口。

---

## 五、明确不在实现范围内的功能

**已实现（演示级，见上文「账号与角色」）**：用户注册（仅普通用户）、登录 / 注销、按角色的数据权限、商家后台（只读 + 维护门店信息）、管理员后台（概览 / 用户管理 / 全平台订单）。

**不在实现范围内**：

1. **账号安全相关**：短信/邮箱验证码、找回密码、第三方登录、口令加盐哈希、令牌签名与有效期、刷新令牌、记住我；
2. **权限粒度**：按钮级细粒度授权、数据字段级脱敏、操作审计日志、商家入驻审核流程；
3. **业务相关**：真实支付与退款、用户发布评论、图片上传、短信与消息推送、物流/发票等；
4. 完整商家经营功能（上下架、改价、库存、核销）与管理员的全量增删改。

> 原型仅面向**普通浏览用户（C 端）** 的主流程演示，外加两个只读为主的运营后台用于展示角色与权限设计。

---

## 六、考核导向：我会主动规避的问题

1. **不写「项目无后端、无架构」** → 统一口径：课程演示原型系统，前后端分离，后端极简演示实现，内存数据源，完整业务不在原型范围内。
2. **不缺多方案对比**（报告评分权重 0.2）→ 必写纯前端 Mock 与极简后端骨架两套方案对比及选型理由。
3. **不缺数据库章节** → 原型不落库 ≠ 不做领域模型与表设计思路。
4. **不出现无法演示的碎片** → 每个业务域都要有可独立演示的完整片段。
5. **不让架构只被一个人理解** → 5 视图架构文档 + 答辩问答要点，所有人可讲。
6. **口径随实现同步更新** → 已补做演示级登录与三角色后台，本文件第三节、第四节（接口表、非功能需求）与第五节均已同步改写；撰写 5 视图架构时须包含**认证层与权限层**、`User`/`Session` 表设计、登录与鉴权时序图。

### 考核权重对应关系（自查用）

| 考核项 | 权重 | 我对应的交付物 |
| --- | --- | --- |
| 实验报告 | 30% | D6 ~ D11（含多方案对比、用例、领域模型） |
| 架构设计方案 | 0.5 | D11（5 视图架构）+ D10（方案对比） |
| 软件文档说明书 | 0.2 | D7 / D8 / D9（用例、领域模型、非功能设计） |
| 软件原型系统开发 | 0.3 | D1 ~ D5（可运行、可演示、可见分工成果） |

---

## 七、当前环境与已完成的准备工作

| 项目 | 状态 |
| --- | --- |
| 工作目录 | `D:\Ctrip`（初始为空目录） |
| Node.js | **v26.4.0（本机实测，经 `node --version`、`process.versions.node`、二进制文件版本三重校验）**，可执行文件 `D:\wxdev\node\node.exe`；全机仅此一处安装、无 nvm 版本管理器 |
| npm | `11.17.0`（随上述 Node 提供） |
| Python | 基础解释器 `C:\Users\王一龙\AppData\Local\Programs\Python\Python312\python.exe`（**3.12.2**）；注意系统 PATH 中的 `python` 是 Microsoft Store 的 0 字节占位符，不是真解释器 |
| Python 虚拟环境 | 已在项目内创建：`D:\Ctrip\.venv`（Python 3.12.2），**仅用于后端依赖隔离**，前端不使用 |
| 后端依赖 | 已安装到 `.venv` 并 import 验证通过：fastapi 0.141.1、uvicorn 0.53.0、pydantic 2.13.5（清单见 `requirements.txt`） |
| 前端依赖 | 待创建工程后由 npm 安装（Vue 3 + Vite + Element Plus + Vue Router + Axios） |
| 需求冻结 | 本文件 `REQUIREMENTS.md` |

> **Node 版本风险与降级预案（可写入报告的运行环境说明）**：本机为 Node.js v26.4.0（较新的偶数版本线）。若搭建 Vite 工程时出现原生依赖（esbuild / rollup）编译或兼容告警，将回退至 Node.js 20 LTS 或 22 LTS 并记录说明，不影响原型功能与架构设计。

---

## 八、技术选型（已确认）

| 层次 | 选型 | 选型理由（可直接写进报告） |
| --- | --- | --- |
| 前端 | **Vue 3 + Vite + Element Plus + Vue Router + Axios** | 组件化开发利于复用导航栏/卡片/星级/弹窗等公共组件；Element Plus 自带日期区间选择器（入住/退房）、筛选、弹窗组件，能快速还原大众点评/携程的列表与筛选交互；Vite 启动快，现场演示无等待。 |
| 后端 | **FastAPI + Uvicorn** | 极简骨架下即可获得清晰的 RESTful 路由与自动生成的 `/docs` 交互式接口文档，现场检查时可直接在浏览器演示接口；Pydantic 模型作为领域对象的代码级映射，便于完成数据架构部分的文档编写（报告中的领域对象模型仍按业务语义即属性与关系描述）。 |
| 数据源 | 后端**内存集合**（Python 列表/字典） | 课程原型演示定位，不落库；下单与取消仅作用于内存集合，重启后新增订单与状态变更全部丢失，需在答辩中主动说明。 |
| 运行方式 | 本地前后端分离，HTTP 通信 | 物理架构即本地运行架构，无需部署远程服务器。 |

### 仍待你提供的信息

1. **是否需要我做「5 视图架构」的架构图**（用 Mermaid 画在 Markdown 里，便于贴进报告）？
2. **报告模板**：如果学校有实训报告册模板文件，发我，我按模板章节逐节填写。

---

## 九、下一步建议的执行顺序

1. 搭建前后端工程骨架 + 公共组件 + 全局路由
2. 铺内存模拟数据（按第三节数据规模下限）与查询接口
3. 逐模块实现：首页 → 本地生活 → 旅行住宿 → 预约下单 → 个人中心
4. 端到端演示联调（按第二节流程 1~8 走通）
5. 补齐 5 视图架构、用例、领域模型、表设计、方案对比等文档
