# 实施步骤（按阶段划分）Execution Plan

> 配套文档：`REQUIREMENTS.md`（需求与交付范围基线，已冻结）
> 技术栈（已确认）：**Vue 3 + Vite + Element Plus + Vue Router + Axios** / **FastAPI + Uvicorn** / 内存数据源 / 本地前后端分离
> 工作目录：`D:\Ctrip`
> 时间线：**阶段 0 → 阶段 10 严格顺序执行**，每个阶段末尾设「验收关口」，未通过不进入下一阶段。

---

## 一、阶段总览

| 阶段 | 名称 | 主要产出 | 完成判据（验收关口） |
| --- | --- | --- | --- |
| 阶段 0 | 需求冻结与环境准备 | `REQUIREMENTS.md`、`.venv`、`requirements.txt` | ✅ **已完成** |
| 阶段 1 | 工程骨架与目录规范 | `backend/` 可启动、`frontend/` 可访问、README 启动说明 | 两条命令分别启动前后端，浏览器可见空白页与 `/docs` |
| 阶段 2 | 领域对象与内存数据层 | 领域对象定义 + 内存集合 + 模拟数据（按规模下限） | 控制台可打印各集合条数且达标 |
| 阶段 3 | 后端查询接口 | I1~I6 查询接口 | `/docs` 中逐个接口 Try it out 返回正确数据 |
| 阶段 4 | 前端公共基础设施 | 路由、Axios 封装、5 个公共组件、全局样式 | 首页可渲染，导航/底部栏/星级/卡片/弹窗均可用 |
| 阶段 5 | 首页模块 | 轮播、分类导航、推荐卡片、搜索结果页 | 搜索关键词可跳转并筛出门店/酒店 |
| 阶段 6 | 本地生活模块 | 门店列表（筛选/排序）、门店详情（套餐/评论） | 筛选与排序切换后列表结果明显变化 |
| 阶段 7 | 旅行住宿模块 | 酒店列表（日历/筛选/排序）、酒店详情（房型/评价） | 日历选入住退房后价格与天数联动 |
| 阶段 8 | 预约下单与个人中心 | I7/I8/I9/I10 接口 + 下单弹窗 + 订单列表/详情/取消 | 下单后立即出现在个人中心，取消后状态变为「已取消」 |
| 阶段 9 | 端到端联调与演示走查 | 演示流程 1~8 全流程通过 | 按 `REQUIREMENTS.md` 第二节流程完整走通一遍不中断 |
| 阶段 10 | 文档产出与答辩准备 | 5 视图架构、用例、领域模型、表设计、方案对比、答辩口径 | 文档章节与考核点逐条对齐，无缺项 |

---

## 阶段 0：需求冻结与环境准备 ✅ 已完成

**目标**：把"做什么"钉死，避免后期返工。

**已完成操作**

1. 起草并冻结需求基线 → `D:\Ctrip\REQUIREMENTS.md`
2. 探测本机环境：Node.js `v26.4.0`（全机唯一、无 nvm）、npm `11.17.0`
3. 定位真实 Python 解释器：`C:\Users\王一龙\AppData\Local\Programs\Python\Python312\python.exe`（3.12.2）
   - ⚠️ 系统 PATH 里的 `python` 是 Microsoft Store 的 **0 字节占位符**，不可用，全程禁用
4. 创建虚拟环境：`D:\Ctrip\.venv`
5. 安装后端依赖并 import 验证：fastapi 0.141.1 / uvicorn 0.53.0 / pydantic 2.13.5
6. 生成 `requirements.txt`、`.gitignore`

**验收关口**：`.venv\Scripts\python.exe -c "import fastapi"` 无报错 → **已通过**

---

## 阶段 1：工程骨架与目录规范

**目标**：搭出前后端两个能跑起来的空壳，固定目录规范，后续所有代码不再挪位置。

### 操作步骤

1. 创建目录骨架（后端）

```powershell
New-Item -ItemType Directory -Force D:\Ctrip\backend\app\api, D:\Ctrip\backend\app\models, D:\Ctrip\backend\app\data, D:\Ctrip\backend\app\core, D:\Ctrip\docs -Force
```

2. 建立后端最小可运行入口 `backend\app\main.py`：注册 FastAPI 实例、开启 **CORS**（允许 `http://localhost:5173`）、挂载路由、根路径返回服务状态
3. 前端工程初始化（在 `D:\Ctrip` 下）

```powershell
cd D:\Ctrip
npm create vite@latest frontend -- --template vue
cd D:\Ctrip\frontend
npm install
npm install element-plus vue-router axios
npm run dev
```

4. 配置 Vite 代理：`/api` → `http://127.0.0.1:8000`（前端统一用相对路径 `/api/xxx` 调用，避免跨域与环境切换问题）
5. 固定目录约定（写进 `README.md`）

```
D:\Ctrip
├─ .venv/                     Python 虚拟环境（仅后端用）
├─ REQUIREMENTS.md            需求基线
├─ PLAN.md                    本文件
├─ requirements.txt           后端依赖清单
├─ README.md                  启动说明（D5）
├─ backend/
│  └─ app/
│     ├─ main.py              服务入口 + CORS + 路由挂载
│     ├─ core/response.py     统一响应 {code,message,data}
│     ├─ models/schemas.py    领域对象（Pydantic）
│     ├─ data/mock_data.py    模拟数据集合
│     ├─ data/store.py        内存集合与读写函数
│     └─ api/                 home.py search.py shops.py hotels.py orders.py
├─ frontend/
│  └─ src/
│     ├─ main.js  App.vue
│     ├─ router/index.js      全局路由
│     ├─ api/request.js       Axios 封装（拦截统一响应结构）
│     ├─ api/*.js             各模块接口调用
│     ├─ components/          NavBar TabBar StarRate CommonDialog ShopCard HotelCard
│     ├─ views/               Home ShopList ShopDetail HotelList HotelDetail Profile OrderDetail
│     └─ styles/              主题色与通用样式
└─ docs/                      报告素材（阶段 10 产出）
```

### 产出物
`backend/` 可启动 + `frontend/` 可访问 + `README.md` 启动说明（交付物 D5）

### 验收关口
- 后端：`D:\Ctrip\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000` → 浏览器打开 `http://127.0.0.1:8000/docs` 可见 Swagger 文档
- 前端：`npm run dev` → 浏览器打开 `http://localhost:5173` 可见页面
- 前端能调通后端一个探针接口（如 `/api/health`）并打印返回

### 风险与预案
- `npm.ps1` / `Activate.ps1` 可能被执行策略拦截 → 用 `npm.cmd`、`.venv\Scripts\activate.bat`，或直接写全路径调用 `python.exe`
- Vite 原生依赖在 Node 26 上告警 → 触发 `REQUIREMENTS.md` 第七节的**降级预案**（回退 Node 20/22 LTS 并记录）

---

## 阶段 2：领域对象与内存数据层

**目标**：数据先立起来，接口才有东西可返回；这是**数据架构**章节的代码支撑。

### 操作步骤

1. 在 `models/schemas.py` 定义领域对象（Pydantic）：`Shop`（门店）、`Package`（团购套餐）、`Hotel`、`RoomType`（房型）、`Comment`（评论/评价）、`Order`（订单）、`Banner`（轮播）、`Category`（分类）
2. 明确对象关系：门店 1—N 套餐、门店 1—N 评论、酒店 1—N 房型、酒店 1—N 评价、订单 N—1 门店/酒店 + N—1 套餐/房型
3. 订单对象必须含：`orderNo`、`type`(shop/hotel)、`targetId`、`targetName`、`itemId`、`itemName`、`name`、`phone`、`bookTime`、`amount`、`status`（待使用/已取消）、`createdAt`
4. 在 `data/mock_data.py` 铺模拟数据，**必须达到规模下限**（`REQUIREMENTS.md` 第三节表格）：

| 数据对象 | 规模下限 |
| --- | --- |
| 门店 | ≥ 12 家（多品类：美食/休闲娱乐；多价位） |
| 酒店 | ≥ 10 家（多星级、多价位） |
| 团购套餐 | ≥ 3 个/门店 |
| 房型 | ≥ 3 种/酒店 |
| 评论 / 评价 | ≥ 5 条/门店、≥ 5 条/酒店 |
| 预置订单 | 2 ~ 3 条 |

5. 在 `data/store.py` 建立**内存集合**（Python 列表/字典）与统一读写函数：`list_shops()`、`get_shop(id)`、`append_order(order)`、`cancel_order(orderNo)`
6. 图片统一用可离线访问的静态占位图或纯 CSS 色块（**不依赖外网图床**，防止断网演示翻车）

### 产出物
领域对象定义 + 内存数据集 + 读写函数（交付物 D3）

### 验收关口
后端启动时控制台打印各集合条数，逐项 ≥ 下限；抽查 3 个门店、3 家酒店的数据完整度（图片、评分、价格、标签、套餐/房型、评论均非空）

### 风险与预案
- 数据量不足会导致"筛选/排序看不出变化"，这是评审最容易扣分的点 → 数据先于页面完成
- 图片若用外链，断网即白屏 → 一律本地占位

---

## 阶段 3：后端查询接口（I1~I6）

**目标**：先把只读接口全部做完，前端才好对接。

### 操作步骤

1. `core/response.py` 实现统一响应 `{code, message, data}`
2. `api/home.py` → **I1 首页推荐**：返回轮播、分类入口、推荐门店、推荐酒店
3. `api/search.py` → **I2 关键词搜索**：按 `keyword` 同时匹配门店名与酒店名，返回两类结果
4. `api/shops.py` → **I3 门店列表**（支持 `keyword`、`category`、`minRating`、`maxPrice`、`sortBy`）；**I4 门店详情**（基础信息 + 套餐 + 评论）
5. `api/hotels.py` → **I5 酒店列表**（支持 `city`、`minPrice`、`maxPrice`、`star`、`facility`、`checkIn`、`checkOut`、`sortBy`）；**I6 酒店详情**（图文 + 房型房价 + 评价）
6. 筛选与排序逻辑全部在后端内存集合上完成（体现"前后端分离 + 接口承担查询"的架构合理性）

### 产出物
I1~I6 查询接口（交付物 D2 的一部分）

### 验收关口
在 `http://127.0.0.1:8000/docs` 中逐个接口 Try it out：入参生效、排序生效、空结果返回空数组而非报错

---

## 阶段 4：前端公共基础设施

**目标**：把公共组件与请求层做完，后面 5 个页面只是"组装"，这是**开发架构**章节的复用论证点。

### 操作步骤

1. `router/index.js` 配置路由：`/`（首页）、`/shops`（门店列表）、`/shops/:id`（门店详情）、`/hotels`（酒店列表）、`/hotels/:id`（酒店详情）、`/profile`（个人中心）、`/profile/orders/:orderNo`（订单详情）、`/search`（搜索结果）
2. `api/request.js`：Axios 实例 + baseURL `/api` + 响应拦截器（统一解包 `{code,message,data}`，失败弹出 Element Plus `ElMessage`）
3. 在 `api/` 下按模块封装调用函数（home / search / shop / hotel / order）
4. 实现 5 个**公共组件**：
   - `NavBar.vue` 顶部导航（含搜索框与返回）
   - `TabBar.vue` 底部栏（首页 / 本地生活 / 旅行住宿 / 个人中心）
   - `StarRate.vue` 星级与评分展示（可复用门店/酒店/评论）
   - `CommonDialog.vue` 通用弹窗（预约下单表单复用）
   - `ShopCard.vue` / `HotelCard.vue` 卡片（列表页复用）
5. `styles/` 定主题色与圆角、阴影规范，统一大众点评（橙色系）+ 携程（蓝色系）风格

### 产出物
路由 + Axios 封装 + 公共组件库（交付物 D1 的基础）

### 验收关口
在首页临时放置 5 个组件均能正常渲染与交互；请求层能拿到后端数据并自动解包

### 风险与预案
- 组件若按页面"分头各写一套"，会直接被认定为复用不足 → 组件必须先于页面完成

---

## 阶段 5：首页模块

**目标**：完成演示动线起点，对应演示流程第 3 步。

### 操作步骤
1. 轮播推荐位（Element Plus `ElCarousel`，2~3 张本地图）
2. 业务分类入口：**美食 / 休闲娱乐 / 酒店**（不设景点分类），点击跳转对应列表页
3. 推荐区：推荐美食门店卡片 + 酒店卡片（复用 `ShopCard` / `HotelCard`）
4. 顶部搜索框：回车跳 `/search?keyword=xxx`，结果页分区展示命中门店与酒店
5. 点击任意卡片进入对应详情页

### 验收关口
分类跳转正确、卡片能进详情、搜索"火锅"等关键词能筛出结果；页面无控制台报错

---

## 阶段 6：本地生活模块（大众点评）

**目标**：对应演示流程第 4 步，本模块要能独立演示。

### 操作步骤
1. 门店列表页：图片、名称、评分、人均、业务标签
2. 筛选区：品类、评分区间、价格区间（人均）；排序：人气优先、评分优先、价格升降
3. 门店详情页：基础信息 + 营业时间 + 地址；团购套餐区（价格、原价、已售）；用户评论区（头像、昵称、评分、内容、时间）
4. 详情页挂"立即预约"按钮 → 触发公共下单弹窗（阶段 8 接通）

### 验收关口
切换品类/评分/价格后列表条数变化可肉眼确认；排序切换顺序明显变化；详情页套餐与评论有数据

---

## 阶段 7：旅行住宿模块（携程）

**目标**：对应演示流程第 5 步，重点是**日历**与**房型**。

### 操作步骤
1. 酒店列表页：图片、名称、星级、价格、服务标签（WiFi/停车场/健身房/泳池等）
2. 日历：Element Plus `ElDatePicker` 日期区间（入住/退房）→ 自动计算**入住晚数**并联动总价
3. 筛选：价格区间、星级、设施（多选）；排序：价格优先、好评优先
4. 酒店详情页：酒店图文信息 + 房型列表（房型名、面积、床型、早餐、价格、预订按钮）+ 酒店评价区
5. 点击房型"预订" → 触发公共下单弹窗，自动带入房型与入住时间

### 验收关口
日历选 2 晚时总价为 `单价 × 2`；筛选/排序生效；房型与评价有数据

---

## 阶段 8：预约下单与个人中心

**目标**：打通**写接口 + 订单闭环**，对应演示流程第 6、7 步；这是最容易出现逻辑断点的环节。

### 操作步骤

1. `api/orders.py` 实现两个写接口 + 两个查询接口：
   - **I7 POST `/api/orders`**：接收预约请求 → 生成订单号 → **将订单对象追加至内存集合** → 控制台打印订单信息 → 返回成功与订单号
   - **I8 GET `/api/orders`**：返回内存集合中的全部订单（原型无登录）
   - **I9 GET `/api/orders/{orderNo}`**：订单详情
   - **I10 POST `/api/orders/{orderNo}/cancel`**：**把内存中该订单 `status` 改为「已取消」** → 返回成功与新状态
2. 下单弹窗（`CommonDialog`）：姓名、手机号、预约/入住时间，**前端做基础必填校验**（不做服务端校验与短信验证）
3. 个人中心页：订单列表（预置订单 + 本次运行新下单）、状态标签（待使用 / 已取消）、订单详情页、取消按钮（二次确认 → 调用 I10 → 刷新列表）
4. 明确并实测原型限制：**服务重启后新增订单与取消状态全部丢失**

### 产出物
I7~I10 接口 + 下单闭环 + 个人中心（交付物 D2 的一部分）

### 验收关口（必须逐条实测）
- 下单一单 → 个人中心**立即出现该订单**（证明内存追加生效）
- 取消该订单 → 状态变「已取消」且刷新后仍是已取消（证明内存改状态生效）
- **重启后端 → 新订单消失、预置订单还在**（证明未持久化，答辩口径成立）

---

## 阶段 9：端到端联调与演示走查

**目标**：把 `REQUIREMENTS.md` 第二节的 8 步流程连续走通，形成答辩动线。

### 操作步骤

1. 严格按顺序走查并计时：启动后端 → 启动前端 → 首页浏览与搜索跳转 → 门店筛选/详情/套餐/评论 → 酒店筛选/日历/房型 → 模拟下单 → 个人中心查看订单并模拟取消 → 口头说明原型限制
2. 逐页排查控制台报错与网络请求失败（F12 Network 全绿）
3. 准备**演示应急预案**：
   - 后端重启后订单丢失属预期，演示单一路线中**不要中途重启后端**
   - 若前端起不来，改用预先构建的 `npm run build` 产物或直接演示后端 `/docs` + 已截图页面
4. 固化**演示当天启动清单**（见文末）

### 验收关口
连续走查 2 遍不卡壳、不报错、无需临场改代码

---

## 阶段 10：文档产出与答辩准备

**目标**：把代码成果翻译成考核要的文档，全部落在 `docs/` 下，可直接粘进实训报告。

### 操作步骤（每份一个 md，建议同时输出 Mermaid 图）

| 序号 | 文档 | 对应考核点 | 要点 |
| --- | --- | --- | --- |
| 1 | `docs/01-引言与整体功能概述.md` | 报告 30% | 项目背景、定位（课程演示原型）、功能结构图 |
| 2 | `docs/02-功能需求与用例.md` | 文档说明书 0.2 | 五大模块功能需求 + 用例说明（用例名/参与者/前置条件/基本流程/后置条件） |
| 3 | `docs/03-非功能需求与非功能设计.md` | 文档说明书 0.2 | 需求 6 条 ↔ 设计对策一一对应 |
| 4 | `docs/04-接口需求.md` | 文档说明书 0.2 | I1~I10 接口表 + 统一响应结构，与代码一致 |
| 5 | `docs/05-五视图架构设计.md` | **架构 0.5（核心）** | 逻辑/开发/数据/运行/物理五视图，各配 Mermaid 图 |
| 6 | `docs/06-领域对象模型与表设计.md` | 文档说明书 0.2 | 领域对象属性与关系 + 完整业务表设计（字段/主键/外键），注明原型用内存、生产可迁移数据库 |
| 7 | `docs/07-技术方案对比与选型.md` | 报告权重 0.2 | 方案1 纯前端 Mock vs 方案2 极简后端骨架，优劣对比 + 选型理由 |
| 8 | `docs/08-原型系统说明与答辩口径.md` | 现场 0.3 / D12 | 启动步骤、演示动线、原型限制、高频提问与回答要点、**团队分工说明与全体成员熟悉 5 视图架构的要求** |

### 验收关口
对照考核权重表自查：架构 0.5 / 文档 0.2 / 原型 0.3 三类材料齐备；文档描述与代码实际行为一致（不写没实现的功能）

---

## 二、演示当天启动清单（固化）

```powershell
# 1. 启动后端（新终端，不要关）
D:\Ctrip\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8000
#    工作目录：D:\Ctrip\backend
#    验证：浏览器打开 http://127.0.0.1:8000/docs

# 2. 启动前端（新终端，不要关）
cd D:\Ctrip\frontend
npm run dev
#    访问：http://localhost:5173

# 3. 演示动线：首页 → 搜索 → 门店筛选/详情 → 酒店日历/房型 → 模拟下单 → 个人中心 → 模拟取消 → 说明原型限制
```

**禁忌**：演示中途不要重启后端（内存订单会清空）；不要现场 `npm install`；不要依赖外网图片。

---

## 三、交付物完成度自查表（对照 `REQUIREMENTS.md`）

| 交付物 | 对应阶段 | 状态 |
| --- | --- | --- |
| D1 前端工程 | 阶段 1 / 4~8 | ☐ |
| D2 极简后端工程 | 阶段 1 / 3 / 8 | ☐ |
| D3 内存模拟数据 | 阶段 2 | ☐ |
| D4 虚拟环境（仅后端用） | 阶段 0 ✅ | ☑ |
| D5 启动说明（README） | 阶段 1 / 9 | ☐ |
| D6 实训报告素材 | 阶段 10 | ☐ |
| D7 需求与用例 | 阶段 10 | ☐ |
| D8 领域对象模型 | 阶段 2 / 10 | ☐ |
| D9 业务表设计思路 | 阶段 10 | ☐ |
| D10 多技术方案对比 | 阶段 10 | ☐ |
| D11 5 视图架构说明 | 阶段 10 | ☐ |
| D12 答辩口径 | 阶段 10 | ☐ |
